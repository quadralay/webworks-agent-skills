# Shell scripts and test fixtures must use LF line endings on all platforms
# so bash and the test driver behave identically across Windows and Unix.
*.sh                                                                    text eol=lf
plugins/webworks-claude-skills/skills/automap/tests/fixtures/**         text eol=lf
